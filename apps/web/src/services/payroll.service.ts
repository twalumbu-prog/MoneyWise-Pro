/** Moved to `core`. Re-exported so existing import paths keep working. */
export { payrollService } from 'core';
export type {
    StaffAllowance,
    StaffDeduction,
    AllowanceConfig,
    DeductionConfig,
    PayrollConfig,
    StaffMember,
    PayrollRun,
    PayrollRunItem,
    PayrollRunDetail,
    PayrollDocument,
    StaffPayrollHistoryItem,
} from 'core';
