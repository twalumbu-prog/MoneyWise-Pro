/** Moved to `core`. Re-exported so existing import paths keep working. */
export { PL_SECTIONS, onboardingService } from 'core';
export type { OnboardingProgress, BusinessProfile, OnboardingState, PlSection, CoaAccount, WalletStatus } from 'core';
