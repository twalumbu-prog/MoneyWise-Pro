/** Moved to `core`. Re-exported so existing import paths keep working. */
export { integrationService, masterFeesService } from 'core';
export type {
    IntegrationStatus,
    MasterFeesStatus,
    MasterFeesCategory,
    MasterFeesReconciliation,
} from 'core';
