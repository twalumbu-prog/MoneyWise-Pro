/** Moved to `core`. Re-exported so existing import paths keep working. */
export {
    REQUISITION_STATUS_CONFIG,
    getStatusConfig,
    requisitionService,
} from 'core';
export type {
    Requisition,
    RequisitionMessage,
} from 'core';
