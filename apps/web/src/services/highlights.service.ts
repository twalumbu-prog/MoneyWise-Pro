/** Moved to `core`. Re-exported so existing import paths keep working. */
export { highlightsService } from 'core';
export type { HighlightCard, HighlightHeadline, HighlightsPayload, Achievement } from 'core';
