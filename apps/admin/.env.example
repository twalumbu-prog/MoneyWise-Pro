# Supabase (used for admin LOGIN / session only — all financial data is fetched
# through the API behind super-admin auth, never read directly with this key).
VITE_SUPABASE_URL=https://klfeluphcutgppkhaxyl.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Base URL of the MoneyWise API that hosts the /admin reconciliation routes.
# Local dev: http://localhost:3000 — Production: your deployed API host.
VITE_API_URL=http://localhost:3000
